<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title | default: siteMetaData.title }}</title>
  </head>
  <body>
    <h1>{{ siteMetaData.title }}</h1>
    {{ content }}
  </body>
</html>
