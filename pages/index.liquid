---
layout: base-layout.liquid
---

<header>
    <nav>
        <span>📚</span>
    </nav>
</header>

<main>
    <h1>Tipiṭaka—the Three Baskets of the Buddhist canon</h1>

    <ul>
    {% for item in indexData %}
        <li>
            <h2><a href="pitaka/{{item.uid}}">{{ item.root_name }}—{{ item.translated_name }}</a></h2>
            <p>{{ item.blurb }} </p>
        </li>
    {% endfor %}
    </ul>

    <script>
    const data = {{ indexData | jsonify }};
    console.log(data);
    </script>
</main>

{% include "components/footer.liquid", route: "" %}
